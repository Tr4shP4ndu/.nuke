set cut_paste_input [stack 0]
version 12.2 v1
push $cut_paste_input
BlinkScript {
 recompileCount 8
 ProgramGroup 1
 KernelDescription "2 \"TransformRandomKernel\" iterate pixelWise 8aa76e8fd745d6ff46dafc3c79eda8366949b07e306cea4793c5b633dcfd79d4 2 \"src\" Read Random \"dst\" Write Point 1 \"offset\" Int 2 AAAAAAAAAAA= 1 \"offset\" 2 1 1 \"srcconstEdgeColor\" Float 4 1 AAAAAAAAAAAAAAAAAAAAAA=="
 kernelSource "\nkernel TransformRandomKernel : ImageComputationKernel<ePixelWise>\n\{\n  Image<eRead, eAccessRandom, eEdgeConstant> src; // the input image\n  Image<eWrite> dst; // the output image\n\n  param:\n    // This parameter is made available to the user.\n\n    int2 offset;\n\n  local:\n    // This local variable is not exposed to the user.\n\n  // In define(), parameters can be given labels and default values.\n  void define() \{\n    \n  \}\n\n  // The init() function is run before any calls to process().\n  // Local variables can be initialized here.\n  void init() \{\n\n    \n \n\n    \n  \}\n\n  void process(int2 pos) \{\n    \n    // Write the result to the output image\n    dst() = src(pos.x+offset.x, pos.y+offset.y);\n  \}\n\};\n"
 rebuild ""
 rebuild_finalise ""
 name BlinkScript5
 selected true
 xpos -236
 ypos -210
}
