#! C:/Program Files/Nuke12.2v1/nuke-12.2.1.dll -nx
version 12.2 v1
define_window_layout_xml {<?xml version="1.0" encoding="UTF-8"?>
<layout version="1.0">
    <window x="7" y="0" w="640" h="480" screen="0">
        <splitter orientation="1">
            <split size="0"/>
            <dock id="" hideTitles="1" activePageId="Toolbar.1">
                <page id="Toolbar.1"/>
            </dock>
            <split size="4" stretch="1"/>
            <splitter orientation="2">
                <split size="355"/>
                <dock id="" activePageId="Viewer.1">
                    <page id="Viewer.1"/>
                </dock>
                <split size="81"/>
                <dock id="" activePageId="Curve Editor.1">
                    <page id="Curve Editor.1"/>
                    <page id="DopeSheet.1"/>
                </dock>
            </splitter>
            <split size="628"/>
            <splitter orientation="2">
                <split size="218"/>
                <dock id="" activePageId="Properties.1">
                    <page id="Properties.1"/>
                    <page id="uk.co.thefoundry.backgroundrenderview.1"/>
                </dock>
                <split size="218"/>
                <dock id="" activePageId="DAG.1" focus="true">
                    <page id="DAG.1"/>
                </dock>
            </splitter>
        </splitter>
    </window>
</layout>
}
Root {
 inputs 0
 name D:/Documents/training/BlinkscriptForBeginners/Classes/NukeScripts/creatingAConstant.v01.nk
 frame 30
 format "1920 1080 0 0 1920 1080 1 HD_1080"
 proxy_type scale
 proxy_format "1024 778 0 0 1024 778 1 1K_Super_35(full-ap)"
 colorManagement Nuke
 workingSpaceLUT linear
 monitorLut sRGB
 int8Lut sRGB
 int16Lut sRGB
 logLut Cineon
 floatLut linear
}
BlinkScript {
 inputs 0
 ProgramGroup 1
 kernelSource "\nkernel SaturationKernel : ImageComputationKernel<ePixelWise>\n\{\n  Image<eRead, eAccessPoint, eEdgeClamped> src; // the input image\n  Image<eWrite> dst; // the output image\n\n  param:\n    float saturation; // This parameter is made available to the user.\n\n  local:\n    float3 coefficients;  // This local variable is not exposed to the user.\n\n  // In define(), parameters can be given labels and default values.\n  void define() \{\n    defineParam(saturation, \"Saturation\", 1.2f);\n  \}\n\n  // The init() function is run before any calls to process().\n  // Local variables can be initialized here.\n  void init() \{\n    // Initialise coefficients according to rec. 709 standard.\n    coefficients.x = 0.2126f;\n    coefficients.y = 0.7152f;\n    coefficients.z = 0.0722f;\n  \}\n\n  void process() \{\n    // Read the input image\n    SampleType(src) input = src();\n\n    // Isolate the RGB components\n    float3 srcPixel(input.x, input.y, input.z);\n\n    // Calculate luma\n    float luma = srcPixel.x * coefficients.x\n               + srcPixel.y * coefficients.y\n               + srcPixel.z * coefficients.z;\n    // Apply saturation\n    float3 saturatedPixel = (srcPixel - luma) * saturation + luma;\n\n    // Write the result to the output image\n    dst() = float4(saturatedPixel.x, saturatedPixel.y, saturatedPixel.z, input.w);\n  \}\n\};\n"
 rebuild ""
 rebuild_finalise ""
 name BlinkScript1
 xpos -860
 ypos -113
}
ColorWheel {
 inputs 0
 gamma 0.45
 name ColorWheel1
 xpos -31
 ypos -249
}
Constant {
 inputs 0
 channels rgb
 color {0.13 0.22 0.25 0.1}
 color_panelDropped true
 name Constant1
 xpos 243
 ypos -188
}
Multiply {
 value 2
 value_panelDropped true
 name Multiply1
 xpos 243
 ypos -62
}
BlinkScript {
 inputs 0
 recompileCount 13
 ProgramGroup 1
 KernelDescription "2 \"BlankKernel\" iterate pixelWise 79a2e3c1e6de446b5523ad561e8d0ad955f5ac9365503014fa5921a85ea152fb 1 \"dst\" Write Point 2 \"Colour\" Float 4 AACAPwAAgD8AAIA/AACAPw== \"Muliply\" Float 4 AACAPwAAgD8AAIA/AACAPw== 2 \"colour\" 4 1 \"mult\" 4 1 0"
 kernelSource "\nkernel BlankKernel : ImageComputationKernel<ePixelWise>\n\{\n  \n  Image<eWrite> dst; // the output image\n\n  param:\n    // This parameter is made available to the user.\n    float4 colour;\n    float4 mult;\n\n  local:\n    // This local variable is not exposed to the user.\n\n  // In define(), parameters can be given labels and default values.\n  void define() \{\n  \n    defineParam(colour, \"Colour\", float4(1.0f));\n    defineParam(mult, \"Muliply\", float4(1.0f));\n    \n  \}\n\n  // The init() function is run before any calls to process().\n  // Local variables can be initialized here.\n  void init() \{\n    \n  \}\n\n  void process() \{\n    \n    // Write the result to the output image\n    dst() = colour*mult;\n  \}\n\};\n"
 rebuild ""
 rebuild_finalise ""
 name BlinkScript3
 xpos -31
 ypos -63
}
Viewer {
 inputs 2
 frame 30
 frame_range 1-100
 colour_sample_bbox {0.1020833328 -0.1395833343 0.1031249985 -0.1385416687}
 name Viewer1
 xpos -150
 ypos 82
}
